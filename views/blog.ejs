<div>
<a class="formlink" href="/form">Add post</a>
<% for(let i = 0; i < posts.length; i++) { %>
			<div class="form">
					<h3 class=""><%= posts[i].title %></h3>

					<h4 class="date"><%= posts[i].date %></h4>

					<p class="body"><%= posts[i].body %></p>

			</div>

<%}%>
</div>
